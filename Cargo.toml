[package]
name = "bdns"
version = "0.1.0"
edition = "2024"

[features]
default = ["openssl"]
openssl = []

[dependencies]
async-trait = "0.1.89"
clap = "4.5.60"
env_logger = "0.11"
http = "1.4.0"
pingora = { version = "0.8.0", features = ["openssl", "lb"] }
pingora-openssl = "0.8.0"
